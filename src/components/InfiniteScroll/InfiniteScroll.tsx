import React from 'react';
import { SearchBar } from '../index';
export const InfiniteScroll = () => {
  return (
    <div>
      InfiniteScroll
      <SearchBar searchData={[]} />
    </div>
  );
};
